# T1.M — 1단계 관문 검수 기록 (2026-09-10)

- 검수: meta 가 직접 돌림. 원 출력은 `2026-09-10-T1M.txt` (같은 폴더).
- 대상: `../prodev/` HEAD `fb3692c` (커밋 여덟: 652fa6e → fb3692c). 작업 트리 깨끗.
- 예측: `../predictions/prodev-prediction.md` 파일 시각 13:53:46, 첫 커밋 652fa6e 는 14:06:27. **예측이 제작보다 앞선다.**
- 관문 표시 `IN-PROGRESS` 05:41:10Z 에 만들고 판정 뒤 지웠다.

## 판정: **통과**

관문 기준 (VERIFICATION 2절): 시험 ≥ 25 · 전부 초록 · 표에 빈 줄 없음 · 예측이 먼저.

| 기준 | 실측 | 근거 |
|---|---|---|
| `npm test` (서버 없이) | tests 78 · pass 78 · fail 0 | T1M.txt "## npm test" |
| `npm run test:server` (임시 서버 자동 기동·내림) | tests 7 · pass 7 · fail 0, 포트 자동, 끝나면 내림 | T1M.txt "## npm run test:server" |
| fixture 가 meta 원본과 같다 | find · hooks · chat 셋 동일 | T1M.txt "## fixture 대조" |
| 하네스 산출물 없음 (1단계 전 하네스 금지) | `.claude/agents` · `.claude/skills` 없음 | T1M.txt 마지막 절 |

## VERIFICATION 2절 표 대조 (줄마다)

| 대상 | 표의 시험 | 실측 (test() 이름, T1M.txt) | 확인 |
|---|---|---|---|
| count.js | 600자 · 901자 · `@TO(x)` · 계측 줄 (4건) | `count — 600자 글` · `901자 글` · `@TO(x) 는 봉투지 글이 아니다` · `맨 끝의 계측 줄은 세지 않는다` | 확인 4/4 |
| chat.js | rooms · search 한 낱말 · AND · --speaker · around · since · tail · show · NFD (9건) | `명령 1/6~6/6` 6 · `명령 곁 --speaker` · `AND 1/2` `AND 2/2` · `NFC` = 10건 | 확인 (9 요구, 10 있음) |
| index.js | 카드 12 → 표 11 · errors 1 · exit 1 · next E-0011 | `index —` 8건: 표 11 · errors 1 (E-0010) · exit 1 · next E-0011 · 파서 셋만 · void 실림 · 위키 2 inbox 1 · 모르는 YAML 알림 | 확인. meta 가 T1.3 때 직접 돌린 `index.js` 출력도 같음 |
| find.js | 물음 10 · 조사 3 · 없는 것 1 · find.log 14줄 | `find [물음]` 10 · `[조사]` 3 · `[없는것]` 1 · `find.log 14줄` · void→대체 | 확인 14/14 (questions.json 을 읽어 test() 를 만든다) |
| peek.js | csv · xlsx · pdf · jpg | `peek csv` 120행 열 7 · `peek xlsx` 시트 2 · `peek pdf` 쪽 수 · `peek jpg` 320×240 · 모르는 형식 exit 0 | 확인 4/4 (+1). 이 기계에 pdfplumber 없음 → pdftotext 길로 돎 |
| intake-copy.js | 같은 이름 두 번 · 경로 밖 파일 | `.v2` · `SHA-256 줄 둘` · `허용 뿌리 밖 거절` · 뿌리 없으면 안 막음 · 원본 잠금 | 확인. meta 가 T1.4 때 임시 폴더로 직접 두 번 들여 SHA 두 줄 확인 |
| session-start.js | 전부 있음 · handoff 만 · 없음 · 60줄 일지 | `있음` · `handoff-compact 만 있음` · `없음` · `60줄 넘는 일지` · `잘림` | 확인 4/4 (+1). meta 가 T1.5 때 직접 셋을 돌려 절의 말이 갈림을 봄 |
| pre-compact.js | 6칸 · 180초 안 · 모의 실패 exit 0 | `여섯 칸` · `thinking 0` · `tool_result 300자` · `요약 실패 exit 0` · `기록 못 읽어도 exit 0`; settings 의 PreCompact timeout 180 | 확인. meta 가 T1.5 때 직접 돌려 기대 항목 일곱 포함, thinking 0 |
| pre-reply.js | 막을 다섯 · 통과 셋 | 13건 각각 (막음 10 · 통과 3) + DB 못 열면 자료 방만 + 방 모르면 안 막음 | 확인 13/13. meta 가 T1.5 때 직접 돌려 exit 와 stderr 한 줄 확인 |
| setup.js | 시험 서버로 rooms · cron | `방 일곱` · `참여 일곱` · `다시 돌려도 안 늚` · `rooms.json 일곱` · `settings 훅 셋·env 셋` · `crontab 두 줄` · `archive` | 확인 7/7 |

## 1단계 세부 예측 대 실측 (`prodev-prediction.md` 표)

| 대상 | 예측 | 실측 | 맞음 |
|---|---|---|---|
| `npm test` | ≥ 25 · 전부 초록 | 78 · 78 · 0 | ○ |
| chat.js | 9건 | 10건 (--speaker 하나 더) | ○ |
| index.js | 표 11 · errors 1 · exit 1 · E-0011 | 같음 | ○ |
| find.js | 14/14 · find.log 14줄 | 같음 | ○ |
| pre-reply.js | 13건, 막을 것 10 · 통과 3 | 같음 | ○ |
| pre-compact.js | 6칸 · thinking 0 · 300자 초과 0 | 같음 (보낸 입력 7568자) | ○ |
| session-start.js | 없음/있음/잘림 셋 | 셋 + handoff 만 + 60줄 일지 | ○ |
| `test:server` | 방 일곱 · 참여 일곱 · crontab 두 줄 | 같음 | ○ |

## setup.js 가 만든 settings.json (meta 가 저장소 사본에서 직접 생성)

allow 41 (crew 템플릿 33 + python3 + 회차 5 원인 넷 덮는 항목; `rm`·`mv` 0) · deny 7 · additionalDirectories 3 · env `PRODEV_BOT` · `PRODEV_PROJECT` · `MINIDISCORD_DB` (`PRODEV_HOOK` 없음) · hooks SessionStart(`startup|resume|clear|compact`) · PreCompact(timeout 180) · PreToolUse(`mcp__minidiscord-channel__reply`) · cron 출력 두 줄.

## 관문 전에 meta 가 고친 것 (기록)

- **fixture 결함 하나** (T1.3 착수 전, 관문 밖): `particle_defects` 가 대화 층에서 봇 코드 17건에 걸려 files.md 에 못 닿음. 원인은 meta 의 참고 판별기가 대화 층을 실제 DB 로 재지 않은 것. 결정: 층 순서 4↔5 (ADR-016, 파일이 대화보다 먼저). questions.json · fixtures/README 고침. 예측(14/14)은 안 고침.
- **meta 의 실수 하나**: T1.3 검산 때 index.js 를 prodev 의 fixture 폴더에 대고 돌려 결과 파일 둘이 생김. prodev 가 지움. 이후 검산은 임시 폴더 사본으로만.
- **지시의 잘못 하나**: T1.6 에 `PRODEV_HOOK` 을 봇 env 에 넣으라 함 → prodev 가 뜻이 뒤집힘을 짚음 → 빼기로.

## 설계에서 달라진 것 (prodev 가 물어서 meta 가 정함, 문서에 반영됨)

- ADR-016 층 순서 · ARCHITECTURE 5.3 (5층 "최근 10건을 골라 오래된 순으로") · 8절 pre-reply "DB 못 열면 자료 방과 보고 방 발송 fail-closed, 방 갈래를 모르면 막지 않음".
- 설계 밖에서 더한 것 (기능 추가, 설계와 충돌 없음): intake 허용 뿌리 검사(`PRODEV_INTAKE_ROOTS`/`MINIDISCORD_BOT_FILES_DIR`) · pre-compact 빈 요약을 실패로 · 모의 claude 는 규칙 추출(`PRODEV_FAKE_CLAUDE=1` 아래서만).

## 빗나간 줄
없음.

## 다음
T2.1 (하네스 입력 준비, meta 지시) → T2.2 (사람이 prodev 세션에서 `/harness:harness`) → T2.3 → T2.M.
