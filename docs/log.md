# 제작 일지

단계가 끝날 때마다 한 절을 더한다 (`TASKS.md` 0절). 코드가 지금 어떻게 생겼나는 `docs/as-built.md`.
1~3단계는 2026-09-10 에 소급해 적었다 — 적을 때 세션 문맥에 있던 것을 파일로 옮긴 것이다.

---

## 1단계 — 부품과 훅 (2026-09-10)

**한 일**: 저장소를 열고, crew 에서 가져올 것을 가져오고, 새 부품을 만들고, 훅 셋을 손으로 놓았다.
하네스를 돌리기 전에 **세는 것과 막는 것**을 먼저 세웠다 — 지침으로 시키면 실패한다는 것이 앞선 실험의 결론이다.

| 커밋 | 무엇 |
|---|---|
| `652fa6e` | T1.1 저장소·부품 옮기기 |
| `194fc56` | T1.2 chat.js 손질 (search 를 AND 로) |
| `9a198e8` | ADR-016 찾기 층 순서 4↔5 |
| `977bdbe` | T1.3 index.js · find.js |
| `6f1d951` | T1.4 peek.js · intake-copy.js · plot.py |
| `22d1668` | T1.5 훅 셋 |
| `48aafa5` | T1.6 setup.js |
| `fb3692c` | T1.x VERIFICATION 2절 보충 |

**시험**: 0 → 78 (`npm test`). 서버 시험은 setup 7건.
**관문**: 1단계 관문 통과 (2026-09-10).
**단계 안에서 설계를 바꾼 것**: ADR-016 — `find.js` 5층 시험에서 `particle_defects` 가 대화 층의 코드 17건에 걸려 사이드카에 닿지 못했다. 파일이 대화보다 먼저다.
**다음에 넘긴 것**: 하네스가 만들 것과 안 만들 것의 목록 (`docs/harness-input.md` 5절).

---

## 2단계 — 하네스 (2026-09-10)

**한 일**: `/harness:harness` 로 스킬 열셋과 에이전트 여섯을 만들고, 설계와 칸칸이 대조하고, 관문을 받았다.

| 커밋 | 무엇 |
|---|---|
| `29dfb02` | T2.1 하네스 입력 준비 |
| `f3c8242` | T2.2 하네스 산출물 (스킬 13 · 에이전트 6 · CLAUDE.md 24줄) |
| `65fa809` | T2.3 스킬 본문 대조 (`docs/skill-matrix.md`, 72칸) |
| `5c7bc7e` | T2.3 분기표 손질 (관문 1차 반려 반영) |
| `57b1b78` | T2.3 skill-matrix 갱신 |

**시험**: 78 그대로 (하네스는 코드를 안 만든다).
**관문**: T2.M **1차 반려** (트리거 18/20) → 고침 → **2차 통과** (20/20).
반려 사유 둘: ① `brief` 행이 "오늘" 한 낱말로 `journal` 행보다 먼저 걸려 cron 18:30 의 "오늘 일지" 가 브리핑으로 갔다 ② `research` 행이 `/리서치` 방으로만 묶여 본방의 "조사해 줘" 가 어디에도 안 걸렸다. 둘 다 분기표의 **행 순서** 문제였고 스킬 본문은 멀쩡했다.
**단계 안에서 정한 것**: ADR-017 — 설계에 자리가 없던 경로 둘.
**다음에 넘긴 것**: 스킬 본문에 훅이 하는 일을 되풀이하지 않는다는 약속 (금칙 grep 0건).

---

## 3단계 — 시험 환경과 재생 (2026-09-10)

**한 일**: 시험 서버를 띄우고, 기동 문서를 못 박고, 재생 도구를 만들고, meta 가 대본 다섯을 돌렸다.
**이 단계에서 처음으로 코드가 사람 손과 만났고, 문서만 읽어서는 안 보이던 결함이 넷 나왔다.**

| 커밋 | 무엇 |
|---|---|
| `fcc01a4` | T3.1 `docs/launch.md` 초안 · T3.2 `scripts/replay.js` |
| `915b0dc` | T3.1 알림 경로 고침 (ADR-018) |
| `5e46546` | T1.6 과제 폴더 deny 제거 (ADR-019) |
| `9b985b0` | T1.5 pre-reply 봉투 벗기기 (ADR-008 보충) |
| `358cc0a` | T3.3 시험 격리 — 실제 `bots/` 를 만지지 않는다 |

**시험**: 78 → 81 (`npm test`) · 7 → 19 (`test:server`).
**관문**: T3.M **통과**. 예측 열넷 중 12 맞음(기준 10) · 채점표 32/42.
빗나간 둘: P7 위키 갱신 값 $0.58(≤0.5) · P8 1~3층 성공률 10/16(≥12).
기록은 meta 쪽 `runs/2026-09-10-T3M.md`.

### 관문 밖에서 고친 것과 까닭

| 무엇 | 어떻게 드러났나 | 까닭 |
|---|---|---|
| 알림 경로 (ADR-018) | 시험 서버에 실제로 쏴 봤다 | 서버가 Bearer 를 안 읽고(401) 글 올리기는 multipart 만 받는다(406). 그대로면 브리핑도 일지도 안 온다 |
| `-F` → `--form-string` | "낸 줄을 그대로 서버에 쏘는" 시험을 쓰다 잡았다 | `-F` 는 `@` 로 시작하는 값을 파일 경로로 읽는다. 멘션은 언제나 `@TO(` 로 시작한다 |
| 과제 폴더 deny (ADR-019) | R1 재생에서 봇이 헌장을 만들고도 못 썼다 | 파일 뿌리가 과제 저장소들의 부모인데 그것을 통째로 막았다 |
| 확정 봉투 벗기기 (ADR-008 보충) | R2 재생에서 "확정" 이 막혔다 | 사람 글은 언제나 봉투로 시작한다. 원문 맨 앞에서 어휘를 찾으면 확정이 영영 안 된다 |

### 이 단계에서 낸 사고

`ADR-019` 를 손으로 확인하려고 **실제 저장소에서** `setup.js` 를 돌렸다. `--project` 만 임시로 두면 격리된다고 여겼는데 봇 폴더는 저장소의 `bots/` 아래에 생긴다. 사람이 쓰던 `bots/prodev-worktogether-비서/` 를 덮어쓰고, "찌꺼기" 라 여겨 지웠다. 토큰·`.mcp.json`·`rooms.json` 이 함께 사라져 봇이 뜨지 않았고, 사람이 웹에서 봇을 지우고 재등록해 되살렸다.

두 자리에서 틀렸다: 격리한다면서 스크립트가 **어디에** 쓰는지 확인하지 않았고, 되돌릴 수 없는 삭제 앞에서 대상이 제가 만든 것인지 확인하지 않았다. 보고에서 "`bots/` 0개" 를 정상으로 읽은 것도 틀렸다 — 그것이 사고의 증거였다.

막은 방법 (`358cc0a`): 시험은 `scripts/` 와 `common/` 만 임시 폴더로 베껴 거기서 `setup.js` 를 돌린다 (`bots/` 는 베낄 목록에 아예 없다). 그리고 마지막 시험 하나가 "시험 전후 실제 `bots/` 목록이 같다" 를 본다. 격리 전 코드에 그 못만 얹어 돌려 실제로 잡히는 것을 확인했다.

**다음에 넘긴 것**: T3.M 이 남긴 발견 여섯 (산출물 첨부 · 압축 알림 · `rooms.json` · intake 파일 이름 · 카드 결과 절과 find 표현 · 약한 지침 셋).

---

## 3단계 뒤 — 발견 여섯 고치기 (2026-09-10, 진행 중)

관문 밖이라 하나씩 커밋한다.

| 커밋 | 무엇 | ADR |
|---|---|---|
| `d09846b` | 산출물을 방에 첨부한다 (스킬 다섯) | ADR-020 |
| `ead4807` | 봇 env 에 `MINIDISCORD_URL` — 압축 알림이 방에 닿게 | — |
| `9f3e9f1` | `rooms.json` 은 방 이름표. 놓친 글은 서버 재배달을 믿는다 | ADR-021 (ADR-006 뒤집음) |
| `27b60e3` | (규칙) `docs/as-built.md` · `docs/log.md` 를 단계 끝마다 갱신한다 | — |
| `92891d6` | intake 가 서버 저장명에서 원래 이름을 되찾는다 (uuid 앞머리만 벗김) | — |
| `fb087c4` | 카드 결과 절에 찾기 실마리 · find 를 먼저 돌린다 (P8 원인 둘) | — |
| `ce6b8c3` | 약한 지침 셋 (한 글에 물음 하나 · 결재 전 일정 · "이어서 합니다") | — |

**시험**: 81 → 84 (`npm test`, intake 셋) · 19 → 20 (`test:server`, MINIDISCORD_URL 하나).

발견 여섯을 고치며 배운 것 하나: 지침을 세울 때는 **무엇을 세는지**와 **무엇이 아닌지**를
함께 적어야 한다. "한 글에 물음 하나" 만으로는 앞서 안 먹혔다. 세는 것이 물음표가 아니라
사람이 답해야 하는 것의 수이고, 되짚기·보기는 물음이 아니라고 적어야 셀 수 있다.

`CLAUDE.md` 열 줄 가운데 한 줄은 **사람 확인을 받고 고쳤다** (2026-09-10):
"넘치면 파일로 옮기고 경로를 준다" → "넘치면 파일로 옮겨 첨부하고 경로를 옆에 준다".
`ARCHITECTURE.md` 7절의 같은 줄도 함께 맞췄다 — 설계 원문과 지침이 갈리면 안 된다.
동료 세션의 요청만으로 고치지 않은 까닭: 그 파일은 이 저장소에서 일하는 세션의 지침이기도 하다.

---

## 3단계 뒤 — R3 · R5 재측정 (2026-09-10)

고침 여덟 뒤 meta 가 대본 둘을 다시 돌렸다. 기록은 meta 쪽 `runs/2026-09-10-R3-2.jsonl` · `R5-2.jsonl`.

**먹은 것**

| 무엇 | 1차 | 재측정 |
|---|---|---|
| 물음에 `find.js` 를 돌렸나 | 12/20 | **20/20** |
| "이어서 합니다" 첫 줄 | 없음 | 압축 뒤 · 재기동 뒤 넷 전부 |
| 산출물 첨부 (ADR-020) | 경로만 | 공급사별 표를 첨부로 |
| 재기동 뒤 놓친 글 | — | 5/5 처리, 첫 답 3분 26초 |
| 일지의 "방마다 마지막 글" (ADR-021) | 없음 | 있음 |

**아직 남은 것**: P8 1~3층 11/16 (1차 10). 규격 이내 개체 · CH-2B · 공급사별 셋은 여전히 원본 csv 로 갔다.
봇이 `#151` 에서 "E-0001 결과 절에 합격 행·공급사 요약을 넣을까요" 를 사람에게 물었다 — 다섯 번째 물음이다.
**사람이 정할 자리**라 meta 도 지시하지 않았고 이 세션도 손대지 않았다.

**재측정이 새로 드러낸 것 둘**

| 무엇 | 원인 | 고침 |
|---|---|---|
| 압축 알림이 또 건너뜀 | `MINIDISCORD_URL` 은 고쳤는데 **방 번호**가 비어 있었다. PreCompact 입력에 `chat_id` 가 없고, `setup.js` 는 설치할 때 방 번호를 모른다 (방은 `setup.js rooms` 가 나중에 만든다) | 훅이 `rooms.json` 의 본방(이름에 갈래가 없는 방)을 읽는다. 순서에 안 기댄다 |
| `.v2` 를 못 쟀다 | 셋째 벌이 같은 파일이라 봇이 들이지 않았다 (판단은 맞다) | 단위 시험 셋으로 갈음. 대본은 meta 가 고친다 |

앞 고침(`ead4807`)의 시험이 `settings.json` 의 **값만** 보고 실제 요청을 안 본 것이 이번에 드러났다.
새 시험 셋은 모의 서버가 **받은 요청**(방 번호 · 쿠키 · multipart · 본문)을 본다.
고치기 전 훅에 대고 돌려 "알림이 안 나갔다 (방 번호를 못 찾았다)" 로 실패하는 것을 확인했다.

**사람이 더한 것 (재측정 뒤)**: 알림을 **둘**로 했다 — 압축 직전에 더해 압축 **직후**에도
"정리가 끝났습니다. 이어서 하려면 말을 걸어 주세요." 한 줄. 압축 뒤에는 사람이 말을 걸어야
봇이 이어서 하므로 사람이 그 시점을 알아야 한다. 껐다 켠 경우에는 안 올린다 (재배달이 있다).
같은 자리에서 두 번 헛디딘 뒤라(URL 없음 · 방 번호 없음), 방 찾기·토큰·보내는 꼴을
`places.js` 한 자리로 옮기고 훅 둘이 그것을 부르게 했다. 두 곳에 적으면 언젠가 갈린다.

**실제 압축에서 확인 (2026-09-10)**: 알림 둘이 다 나갔다.
본방 `#170` "문맥을 정리 중입니다" (`handoff-compact.md.log` 11:52:19 `ok 알림:보냄`)
→ 압축 2분 15초 → `#171` "정리가 끝났습니다. 이어서 하려면 말을 걸어 주세요"
(`.log` 11:54:34 `session-start 알림:보냄`). `source: compact` 가 실제로 온다.

**알아 둘 성질 하나** (결함이 아니다): 새 세션에서 바로 `/compact` 를 치면
"Not enough messages to compact" 로 압축이 **안 일어나는데** PreCompact 훅은 돈다.
그래서 직전 알림("정리 중입니다")만 나가고 직후 알림은 따라오지 않는다 (`#157` 이 그 자리다).
사람 눈에는 "정리 중" 뒤로 아무 말이 없는 것으로 보인다 — 봇은 멀쩡하니 그냥 말을 걸면 된다.
훅은 압축이 실제로 일어날지 미리 알 수 없으므로 이 자리는 그대로 둔다.

**실전 이전 준비 (2026-09-10)**: 사람이 `crew-workspace` 를 회사 기계로 옮겨 실전 과제(T4)에 붙인다.
`docs/launch.md` 에 10절 "다른 기계로 옮길 때" 를 더했다 — 가져갈 것과 안 가져가도 되는 것 ·
**봇 토큰이 걸리는 자리 셋** · 절대 경로가 다시 쓰이는 파일 넷 · 새 기계에 있어야 하는 것 ·
실전 서버 값 · 계정과 토큰 넣는 자리 둘 · cron 붙이기 · 옮긴 뒤 확인 열두 걸음.
사본에서 옛 경로 → 새 경로로 `setup.js` 를 두 번 돌려, 재실행 한 번에 절대 경로가 전부
다시 쓰이고 옛 경로가 0건 남는 것을 확인했다. 토큰이 없으면 `.mcp.json` 이 안 생기는 것도 확인했다.
포트와 과제 경로는 **회사에서 사람이 정할 값**이라 예시로 남기고 그렇게 밝혔다.
포트를 바꿀 때 어긋나기 쉬운 세 자리(서버 · `setup.js` 의 `MINIDISCORD_URL` · cron 두 줄)를
표로 적고, 하나만 어긋났을 때 무엇이 보이는지도 남겼다 — 셋 다 **조용히** 안 되는 자리다.
사본에서 `MINIDISCORD_URL` 하나가 settings 의 env 와 cron 줄 양쪽으로 흐르는 것을 확인했다.

---

## 방 둘로 줄임 (ADR-022) — 2026-09-10

**한 일**: 과제 하나가 쓰는 방을 7 개에서 2 개로 줄이기로 사람이 정했다. 이 절의 커밋은 **설계 문서만** 그 결정에 맞춘 것이다. 코드 · 스킬 · 시험은 손대지 않았다.

**까닭이 된 숫자** 3단계 재생 다섯에서 방마다 실제로 오간 글: 본방 60 · 들이기 23 · 리서치 14 · 자료 1 · 특허 0 · 논문 0 · 보고 0. 갈래 방 셋(특허 · 논문 · 보고)은 한 글도 받지 못했다. 봇은 방이 아니라 `PRODEV_PROJECT` 로 과제를 알고(ADR-012), 찾을 때도 `find.js` 와 색인 파일을 쓴다 — 방을 나눠서 봇이 얻는 것이 없었다. 게다가 비서 지침("갈래 방은 사람이 허락해야 연다")과 `setup.js`("방을 다 만든다")가 서로 어긋나 있었다.

**바뀐 것** 본방 `prodev-<과제>` 와 `prodev-<과제>/files` 둘만 남는다. 사람이 외우는 규칙은 "말은 아무 데서나, 파일은 files 에" 한 줄이다. 봇은 어느 방에서든 답하고, 본방에 파일이 오면 안내 한 줄만 한다. 확정된 카드는 `files` 방에 **한 줄**로 공지한다 (번호 · 제목 · 경로).

**훅에서 바뀌는 것** 확정 관문의 방아쇠가 **방에서 표식으로** 옮겨 간다 — `[카드]` 로 시작하는 공지 글과 `[발송]` 표식이 있는 글에만 걸린다. 방이 하나로 합쳐지면 방으로는 "공지 글"과 "카드 번호를 말하는 평범한 답"을 가를 수 없기 때문이다. 다섯 조건 자체는 그대로고, ② 의 방 이름만 `/files` 가 된다.

| 커밋 | 무엇 |
|---|---|
| `ffb47ae` | 설계 문서 (PRD · ARCHITECTURE · ADR-022 · TASKS · VERIFICATION · README · docs 다섯) |
| (뒤에 채움) | 코드 · 스킬 · 시험 — meta 가 "문서 PR 통과"를 보낸 뒤 |
